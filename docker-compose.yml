version: "3"

services:
  mcacorp_nginx:
    hostname: mcacorp_nginx
    build: .
    ports:
      - "${PORT}:80"
      - "443:433"

    volumes:
      - webcode:/code

volumes:
  webcode:
      external: true

networks:
    default:
        external:
            name: mca-corp